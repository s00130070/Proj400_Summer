@model Proj400.Models.CartIndex
<div class="wrapper">
    <div class="checkout container">
        <header>
            <h1>Payment Method</h1>
           
        </header>
        <form id="payment-form" method="post" action="/Cart">
            <section>
               
                <div class="bt-drop-in-wrapper">
                    <div id="bt-dropin"></div>
                </div>
                @*<label value="Transamount"></label>*@
                @*@Model.Cart.ComputeToatalValue().ToString("c")*@
                <label for="amount">
                    <span class="input-label">Amount</span>
                    <div class="input-wrapper amount-wrapper">
                        <input id="amount" name="amount" type="tel" min="1" placeholder="Amount" value="10">
                    </div>
                </label>
            </section>
            <button class="button btn-success" type="submit"><span>Purchase</span></button>
        </form>
    </div>
</div>
<script src="https://js.braintreegateway.com/v2/braintree.js"></script>
<script>
    var Cart = new Demo({
        formID: "payment-form"
    });
  var client_token = "@ViewBag.ClientToken";
  braintree.setup(client_token, "dropin", {
    container: "bt-dropin"
  });

    @*var Transamount = @Model.ComputeToatalValue()*@
</script>


